CREATE OR REPLACE TABLE FITNESS_MONTHLY_SUMMARY AS SELECT MONTH,YEAR,SUM(STEPS) TOTAL_STEPS,SUM(KM) TOTAL_KM,SUM(CALORIES) TOTAL_CALORIES,AVG(STEPS) AVG_STEPS FROM FITNESS_CURATED GROUP BY MONTH,YEAR;